### SSM（Split-Shuffle Mechanism）机制详解  


#### **1. SSM的设计背景与目标**  
传统联邦学习中，客户端直接向服务器传输模型明文存在隐私泄露风险（如恶意服务器可通过梯度反演攻击重构训练数据）。SSM通过**模型拆分、随机洗牌和分布式聚合**，在不依赖可信第三方的前提下保护模型隐私。  


#### **2. SSM的核心流程**  
##### **步骤1：模型拆分**  
客户端 \(n_i\) 将本地模型 \(w_i^m = \{e_i^m, d_i^m, c_i^m\}\)（对应模态 \(m\)）拆分为 \(S_i \leq \lambda\) 个随机分量：  
\[
S_i^m = \{s_{i,j}^m | j \in [1, S_i]\}, \quad w_i^m = \sum_{j=1}^{S_i} s_{i,j}^m
\]  
其中 \(\lambda\) 为中继节点总数，\(s_{i,j}^m\) 是模型参数的随机拆分向量。  

##### **步骤2：传输洗牌**  
- 客户端从 \(\lambda\) 个中继节点中随机选择 \(S_i\) 个，将拆分后的分量 \(s_{i,j}^m\) 与集群标签（初始随机分配，后续通过ASCC更新）分别发送至这些节点。  
- 中继节点根据集群标签聚合收到的分量，生成中间结果 \(\chi_{i,j}^m\)，并转发至服务器。  

##### **步骤3：服务器聚合**  
服务器将各中继节点的中间结果聚合为最终的集群模型，全程无法获取完整的客户端模型明文。  


#### **3. SSM的隐私安全性分析**  
##### **定理1：部分中继节点 collusion 时的安全性**  
当服务器与 \(j < S_i\) 个中继节点合谋时，无法恢复客户端 \(n_i\) 的本地模型，绝对安全概率为：  
\[
\sum_{j=0}^{S_i-1} \binom{\lambda}{j} \rho^j (1-\rho)^{\lambda-j}
\]  
其中 \(\rho\) 为单个中继节点的合谋概率，\(\binom{\lambda}{j}\) 为组合数。  

##### **定理2：完全合谋时的模型恢复概率**  
当服务器与 \(j \geq S_i\) 个中继节点合谋时，恢复客户端模型的概率为：  
\[
\sum_{j=S_i}^{\lambda} \binom{\lambda-S_i}{j-S_i} \rho^j (1-\rho)^{\lambda-j}
\]  

##### **定理3：系统级隐私泄露期望**  
当 \(N\) 个客户端参与时，隐私泄露量（PLA）的数学期望为：  
\[
\sum_{i=1}^{N} \sum_{j=S_i}^{\lambda} \binom{\lambda-S_i}{j-S_i} \rho^j (1-\rho)^{\lambda-j} |w_i^m|
\]  
其中 \(|w_i^m|\) 为本地模型 \(w_i^m\) 的参数总量。  


#### **4. SSM的优势**  
- **轻量级实现**：无需复杂密码学操作，适合计算资源有限的中继节点（如路由器）。  
- **抗合谋性**：即使部分中继节点与服务器合谋，仍能以高概率保护模型隐私。  
- **兼容性**：与ASCC算法结合，支持分布式聚类与模型聚合的隐私保护。  


### ASCC（Attention-based Spontaneous Client Clustering）算法流程  


#### **1. ASCC的核心目标**  
解决联邦学习中客户端数据分布异质性问题，通过注意力机制实现**自发聚类、分层个性化和隐私保护**，无需服务器参与聚类决策。  


#### **2. 算法流程详解**  
##### **输入**：  
- 集群编码器 \(\{e_1^m, \dots, e_K^m\}\)（模态 \(m\)，\(K\) 个集群）；  
- 编码器层数 \(L_m\)；  
- 查询数据采样率 \(\beta\)；  
- 标记数据集 \(D_i^{m,l}\)；  
- 学习率 \(\mu\)。  

##### **输出**：  
客户端 \(n_i\) 的个性化本地模型 \(e_i^m\) 和分类器 \(c_i^m\)。  


#### **3. 具体步骤**  
##### **阶段1：注意力训练（Attention Training）**  
1. **参数冻结与初始化**：  
   - 冻结所有集群编码器 \(\{e_1^m, \dots, e_K^m\}\) 和分类器 \(c_i^m\) 的参数；  
   - 初始化注意力参数 \(A_i^m = \{A_i^{m,1}, \dots, A_i^{m,L_m}\}\)，其中 \(A_i^{m,l} = \{a_i^{m,l,1}, \dots, a_i^{m,l,K}\}\) 对应第 \(l\) 层的 \(K\) 个注意力权重。  

2. **查询数据采样**：  
   从 \(D_i^{m,l}\) 中按比例 \(\beta\) 采样查询集 \(Q_i^{m,0} = \{q_{i,j}^{m,0}, y_{i,j}^m\}\)，其中 \(q_{i,j}^{m,0}\) 为原始数据特征。  

3. **迭代前向计算与损失优化**：  
   对每个查询样本 \(q_{i,j}^{m,0}\)，逐层计算：  
   - **第 \(l\) 层处理**：  
     1. 归一化注意力权重：  
        \[
        \{a_i^{m,l,1}, \dots, a_i^{m,l,K}\} = \text{Softmax}(A_i^{m,l})
        \]  
     2. 计算中间特征：  
        \[
        f_{i,j}^{m,l,k} = e_k^{m,l}(q_{i,j}^{m,l-1})
        \]  
     3. 加权聚合生成新查询：  
        \[
        q_{i,j}^{m,l} = \sum_{k=1}^K a_i^{m,l,k} \cdot f_{i,j}^{m,l,k}
        \]  
   - **最终输出与损失计算**：  
     \[
     q_{i,j}^{m,\text{out}} = q_{i,j}^{m,L_m}
     \]  
     注意力损失（交叉熵）：  
     \[
     L_{\text{att}}^j = F_{\text{CE}}\left(c_i^m(q_{i,j}^{m,\text{out}}), y_{i,j}^m\right)
     \]  
   - **参数更新**：  
     最小化总损失 \(L_{\text{att}} = \sum_{j=1}^{|Q_i^{m,0}|} L_{\text{att}}^j\)，更新注意力参数：  
     \[
     A_i^{m*} = \underset{A_i^{m,1},\dots,A_i^{m,L_m}}{\text{arg min}} \ L_{\text{att}}
     \]  


##### **阶段2：聚合与聚类（Aggregation and Clustering）**  
1. **个性化编码器生成**：  
   用更新后的注意力参数作为权重，聚合集群编码器：  
   \[
   e_i^m = \bigoplus_{l=1}^{L_m} \sum_{k=1}^K a_i^{m,l,k} \cdot e_k^{m,l}
   \]  
   其中 \(\oplus\) 表示参数层的拼接操作。  

2. **集群重选择**：  
   计算各集群的累积注意力值，选择适配度最高的集群：  
   \[
   k_{\text{max}}^i = \underset{k}{\text{arg max}} \left\{ \sum_{l=1}^{L_m} a_i^{m,l,k} \right\}
   \]  


##### **阶段3：本地模型微调（Local Model Fine-tuning）**  
1. **解冻参数**：  
   解冻个性化编码器 \(e_i^m\) 和分类器 \(c_i^m\) 的参数。  

2. **全量数据训练**：  
   对所有标记数据 \(D_i^{m,l}\)，计算分类损失并更新模型：  
   - 分类损失：  
     \[
     L_{\text{cls}}^j = F_{\text{CE}}\left(c_i^m(e_i^m(x_{i,j}^m)), y_{i,j}^m\right)
     \]  
   - 梯度下降更新：  
     \[
     e_i^m \leftarrow e_i^m - \mu \nabla L_{\text{cls}}(e_i^m)
     \]  
     \[
     c_i^m \leftarrow c_i^m - \mu \nabla L_{\text{cls}}(c_i^m)
     \]  


#### **4. ASCC的关键特性**  
- **隐私保护**：无需披露模型明文或数据分布，仅通过注意力权重隐式反映数据适配度。  
- **分层个性化**：不同网络层可分配不同的注意力权重，适应数据异质性对深层网络的影响。  
- **自发聚类**：客户端通过本地训练自主选择最优集群，无需服务器集中控制。  
- **计算高效**：训练时冻结集群编码器参数，减少反向传播计算量。